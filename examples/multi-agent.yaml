# bento.yaml for a project using multiple agents simultaneously
# Auto-detection merges layers from all detected agents
agent: auto
task: "multi-agent feature development"

ignore:
  - "*.log"
  - "tmp/"

retention:
  keep_last: 20
  keep_tagged: true
